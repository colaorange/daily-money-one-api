
export const AccountTypeRefPath = '#/components/schemas/AccountType'

export enum AccountType {
    INCOME = 'income',
    ASSET = 'asset',
    EXPENSE = 'expense',
    LIABILITY = 'liability',
    OTHER = 'other'
}